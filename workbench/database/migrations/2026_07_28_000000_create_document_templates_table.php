<?php

// The package ships this migration as a .stub, so the migrator cannot discover
// it. Workbench loads it exactly like a host app would after publishing.
return require __DIR__ . '/../../../database/migrations/create_document_templates_table.php.stub';
